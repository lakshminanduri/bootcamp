---
### Live profile
spring:
  profiles: live
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/transaction_category_collector?useSSL=false

backbase:
  category:
    management:
      enabled: "true"
  transaction:
    enrichment:
      provider: "mock"